# Coding Agent Entry Point

Read `docs/ai-coding/12_CODING_AGENT_MASTER_PROMPT.md`, the latest architecture/roadmap under `docs/architecture` + `docs/roadmap`, and the latest audit before changing product behavior.

For self-hosted server, remote desktop, cross-device or Android work, also read and follow `docs/ai-coding/13_SELF_HOSTED_CROSS_DEVICE_ANDROID_PROMPT.md` before editing code or deployment configuration.

Never collapse Interest Area, Capability Plugin, Domain Pack and Capability Provider back into one layer. Psychology is the default Domain Pack, not the Core. Preserve Area isolation, evidence verification, provider independence, human review, pause/return, desktop token/App-Data/credential boundaries and exact migration compatibility.
