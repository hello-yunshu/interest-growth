# ADR-0003 · Product features are plugin-first

**Status:** Accepted

New domain features should be first-party product plugins with manifest dependencies/permissions/risks rather than additions to an ever-growing Core. The plugin runtime is independent of DeepTutor's loader.
